---

# The deployment relies on the presence
# of a PVC for use as the data volume.
# The PVC (and its project) are expected to already exist.

- hosts: localhost
  tasks:
  - include_role:
      name: graph
